# Neon Postgres (pooled connection string)
DATABASE_URL=

# Anthropic Claude API — paper metadata extraction + grounded Q&A (Phase 6),
# workout analysis (Phase 7+)
ANTHROPIC_API_KEY=

# Vercel Blob — original PDFs of the paper library (Phase 6). Created by
# connecting the "betri-companion-papers" private store to the project.
BLOB_READ_WRITE_TOKEN=

# --- Added in later phases ---
# Embedding provider for pgvector retrieval, once the library outgrows
# catalog-based selection: "voyage" | "openai"
# EMBEDDING_PROVIDER=
# VOYAGE_API_KEY=
# Strava OAuth (when API access is approved)
# STRAVA_CLIENT_ID=
# STRAVA_CLIENT_SECRET=
# Garmin (when partner program is approved)
# GARMIN_CONSUMER_KEY=
# GARMIN_CONSUMER_SECRET=
